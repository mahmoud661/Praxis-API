fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1
aiokafka==0.10.0
asyncpg==0.29.0
structlog==24.1.0
httpx==0.27.0
# Real schema migrations. Alembic uses a sync driver under the hood — the
# app code keeps using asyncpg at runtime; psycopg2 is only loaded for
# `alembic upgrade head` at boot.
alembic==1.13.1
psycopg2-binary==2.9.9
# Python 3.12 ships without `pkg_resources`; opentelemetry-instrumentation imports it.
setuptools==70.0.0
# OpenTelemetry — zero-code auto-instrumentation. The Dockerfile launches
# the app via `opentelemetry-instrument`, which detects installed libraries
# (FastAPI, asyncpg, …) and patches them automatically. No code in app/.
opentelemetry-distro==0.46b0
opentelemetry-exporter-otlp-proto-http==1.25.0
opentelemetry-instrumentation-fastapi==0.46b0
opentelemetry-instrumentation-asyncpg==0.46b0
opentelemetry-instrumentation-logging==0.46b0
# Test runner. Kept in the image so the Dockerfile can `RUN pytest` in the
# build stage to gate startup on a passing unit-test suite.
pytest==8.2.0
pytest-asyncio==0.23.6
