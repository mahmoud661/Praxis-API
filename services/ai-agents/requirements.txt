fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.10.5
pydantic-settings==2.7.1
python-dotenv==1.0.1
python-multipart==0.0.29
aiokafka==0.10.0
# LangGraph persistence — same patterns as the auth service's TypeORM
# `synchronize`. Both call `setup()` on startup, no Alembic, no SQLAlchemy.
# Versions aligned with the Mustang-AMS reference project (langchain 1.x /
# langgraph 1.x APIs).
langgraph>=1.0.5
# langgraph-cli[inmem]>=0.4.8 — DEV-ONLY (the `langgraph dev` local runner).
# Pinning would force starlette>=0.38, which conflicts with the fastapi
# version we're on. The runtime serves via uvicorn directly, so we don't
# need the CLI in the image.
langgraph-checkpoint-postgres
psycopg[binary,pool]==3.3.4
asyncpg>=0.31.0
# LangChain — the agentic runtime uses these for `init_chat_model`,
# middleware types, tool nodes. langchain-core resolves with langchain.
langchain>=1.2.0
langchain-core>=1.2.4
langchain-openai>=1.2.1
# Redis — per-run event stream + per-user notifications pub/sub.
redis[hiredis]==5.0.8
structlog==24.1.0
httpx==0.27.0
# Python 3.12 ships without `pkg_resources`; opentelemetry-instrumentation imports it.
setuptools==70.0.0
# OpenTelemetry — zero-code auto-instrumentation. The Dockerfile launches
# the app via `opentelemetry-instrument`, which detects installed libraries
# (FastAPI, psycopg, …) and patches them automatically.
opentelemetry-distro==0.46b0
opentelemetry-exporter-otlp-proto-http==1.25.0
opentelemetry-instrumentation-fastapi==0.63b1
opentelemetry-instrumentation-psycopg==0.46b0
opentelemetry-instrumentation-logging==0.46b0
# Test runner. Kept in the image so the Dockerfile can `RUN pytest` in the
# build stage to gate startup on a passing unit-test suite.
pytest==8.2.0
pytest-asyncio==1.4.0
