# Reusable integration-test workflow. Runs tests that spin up real
# containers via testcontainers (e.g. real Postgres for repository
# round-trips). Kept separate from the image-build gate so a slow or
# flaky integration test doesn't block the image.

name: integration

on:
  workflow_call:
    inputs:
      service-name:
        required: true
        type: string
      working-dir:
        required: true
        type: string
      runtime:
        required: true
        type: string
        description: "node | python"
      node-version:
        required: false
        type: string
        default: "20"
      python-version:
        required: false
        type: string
        default: "3.12"
      script:
        required: false
        type: string
        default: ""
        description: "Command to run. Defaults vary by runtime."

jobs:
  integration:
    name: "integration ${{ inputs.service-name }}"
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Setup Node
        if: inputs.runtime == 'node'
        uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.node-version }}
          cache: "npm"
          cache-dependency-path: ${{ inputs.working-dir }}/package-lock.json

      # NOTE: setup-uv@v3 does NOT accept a `python-version` input. We use
      # actions/setup-python to pin the interpreter, then setup-uv just
      # installs the `uv` binary on top.
      - name: Setup Python
        if: inputs.runtime == 'python'
        uses: actions/setup-python@v5
        with:
          python-version: ${{ inputs.python-version }}

      - name: Setup uv
        if: inputs.runtime == 'python'
        uses: astral-sh/setup-uv@v3

      - name: Install (Node)
        if: inputs.runtime == 'node'
        working-directory: ${{ inputs.working-dir }}
        run: npm install --include=dev

      # Install into a uv-managed venv (NOT --system). Ubuntu 24.04 marks
      # `/usr` as externally-managed (PEP 668), so --system is blocked.
      # `uv run <cmd>` automatically uses the .venv created by `uv venv`.
      - name: Install (Python)
        if: inputs.runtime == 'python'
        working-directory: ${{ inputs.working-dir }}
        run: |
          uv venv .venv
          uv pip install -r requirements.txt

      - name: Run integration tests
        working-directory: ${{ inputs.working-dir }}
        env:
          DEFAULT_SCRIPT: "${{ inputs.runtime == 'node' && 'npm run test:integration' || 'uv run pytest tests-integration' }}"
        run: |
          CMD="${{ inputs.script }}"
          if [ -z "$CMD" ]; then CMD="$DEFAULT_SCRIPT"; fi
          echo "Running: $CMD"
          eval "$CMD"
